using System;

namespace Lab_3
{
    class Program
    {
        static void Main(string[] args)
        {
            double a, b, c, d, e;
            double f, g, h;

            a = 1;
            b = 2;
            c = 3;
            d = 4;
            e = 5;


            f = (c / a) - (3 * Math.Pow(b, 2)) / (8 * Math.Pow(a, 2));
            g = (d / a) + Math.Pow(b, 3) / (8 * Math.Pow(a, 3)) - (b*c)/(2*Math.Pow(a,2));

            

        }
    }
}
